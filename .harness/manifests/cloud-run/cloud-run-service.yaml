apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: nexapay-bank-cr-service
spec:
  template:
    spec:
      containers:
        - image: <+artifacts.primary.image>
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
